// ============================================================================
// CONFIGURAÇÃO DO SUPABASE — ARQUIVO DE EXEMPLO (pode ser commitado)
// ----------------------------------------------------------------------------
// PASSO A PASSO:
// 1. Copie este arquivo para "js/config.js"  (js/config.js está no .gitignore
//    e NUNCA deve ser enviado ao GitHub).
// 2. No Supabase, acesse: Project Settings > API (ou Dashboard > Settings > API)
// 3. Copie a "Project URL" e a chave pública (anon / publishable) para os
//    campos abaixo.
// 4. Salve — o painel estará pronto para uso.
//
// SEGURANÇA: use APENAS a chave pública (anon / sb_publishable_*).
// Chaves secretas (sb_secret_* / service_role) NUNCA devem ser usadas ou
// commitadas em arquivos de frontend — elas dão acesso total ao banco.
// ============================================================================

const SUPABASE_URL = 'SEU_SUPABASE_URL_AQUI';
const SUPABASE_ANON_KEY = 'SUA_SUPABASE_ANON_KEY_AQUI';
